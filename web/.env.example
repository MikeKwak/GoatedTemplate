# Web Application Environment Configuration
# Copy this file to .env.local and update with your actual values
# DO NOT commit .env.local files to version control
# 
# Note: Next.js requires NEXT_PUBLIC_ prefix for client-side accessible variables

# API Configuration
# Base URL for the FastAPI backend server
# This is accessible on the client-side (browser) because of NEXT_PUBLIC_ prefix
# Update this to match your backend server URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: Environment identifier
# Used for feature flags, analytics, etc.
# NEXT_PUBLIC_ENVIRONMENT=development
