# Mobile Application Environment Configuration
# Copy this file to .env and update with your actual values
# DO NOT commit .env files to version control
#
# Note: React Native apps typically use react-native-config or similar
# to load environment variables. Adjust based on your setup.

# API Configuration
# Base URL for the FastAPI backend server
# Update this to match your backend server URL
# For local development, use your machine's IP address instead of localhost
# Example: http://192.168.1.100:8000 (replace with your local IP)
API_BASE_URL=http://localhost:8000

# Optional: Environment identifier
# Used for feature flags, analytics, etc.
# ENVIRONMENT=development

# Optional: API Timeout (in milliseconds)
# Default timeout for API requests
# API_TIMEOUT=30000
